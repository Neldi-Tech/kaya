import { AdminStub } from '@/components/admin/AdminStub';

export default function AdminPricingPage() {
  return <AdminStub title="Pricing" emoji="💰" summary="Per-currency overrides + Stripe Product/Price wiring. Lands with the paid funnel." />;
}
