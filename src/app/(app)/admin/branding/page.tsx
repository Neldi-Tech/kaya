import { AdminStub } from '@/components/admin/AdminStub';

export default function AdminBrandingPage() {
  return <AdminStub title="Branding" emoji="🎨" summary="Theme palette, font customisation, family colour packs." />;
}
